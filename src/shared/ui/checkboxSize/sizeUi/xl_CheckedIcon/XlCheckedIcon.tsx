import styles from '../styles.module.css'

const XlCheckedIcon = () => {
    return (
        <div className={styles.activeIcon}>
            XL
        </div>
    );
};

export default XlCheckedIcon;