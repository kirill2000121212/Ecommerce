import styles from './styles.module.css'

const AppliedFilters = () => {
    return (
        <div>
            <p className={styles.appliedFiltersText}>Applied Filters:</p>
            {

            }
        </div>
    );
};

export default AppliedFilters;